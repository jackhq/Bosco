%p
  %label{:for => "#{form}_#{name}"}= title
  %br
  - options.each do |o|
    %input{:type => 'radio', :id => "#{form}_#{name}", :name => "#{form}[#{name}]", :value => o, :selected => value == o ? 'true' : false }
    %label= o
    %br
